package work;

import javax.swing.tree.DefaultMutableTreeNode;

import app.Core;
import model.ModelElement;
/**
 * Ovde ide opis 
 * @author Nemanja
 */
public class CreateNewElementOnElement {
	/**
	 * Ovde ide opis 
	 * @author Nemanja
	 */
	public CreateNewElementOnElement(DefaultMutableTreeNode parentAsNode){
//		ModelElement parent = (ModelElement) parentAsNode.getUserObject();
//		ModelElement child = new ModelElement(CreateName.getElementName(), parent.getType());
//		DefaultMutableTreeNode childAsNode = new DefaultMutableTreeNode(child);
//		
//		parent.addElementToList(child);
//		parentAsNode.add(childAsNode);
		
//		Core.getInstance().getView().getTree().getTreeModel().reload(parentAsNode);
	}
}
