package work;

import javax.swing.tree.DefaultMutableTreeNode;

import app.Core;
import model.ModelElement;
import model.ModelSlot;
/**
 * Ovde ide opis klase
 * @author Nemanja
 */
public class CreateNewElementOnSlot {
	/**
	 * Ovde ide opis 
	 * @author Nemanja
	 */
	public CreateNewElementOnSlot(DefaultMutableTreeNode parentAsNode){
//		ModelSlot parent = (ModelSlot) parentAsNode.getUserObject();
//		ModelElement child = new ModelElement(CreateName.getElementName(), Type.type);
//		DefaultMutableTreeNode childAsNode = new DefaultMutableTreeNode(child);
//		
//		if(parent.addElementToSlot(child))
//			parentAsNode.add(childAsNode);
		
//		Core.getInstance().getView().getTree().getTreeModel().reload(parentAsNode);
	}
}
